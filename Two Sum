class Solution {
    public int[] twoSum(int[] nums, int target) {
        Map< Integer, Integer> number=new HashMap<>();
        for(int i=0;i<nums.length;i++){
            if (nums.containsKey(target-num[i])){
                return new int[] {}
            }
        } 
    }
}
